{
  "name": "drome",
  "private": false,
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --filter=!repl-test",
    "dev:repl": "turbo run dev --filter=repl-web",
    "dev:demos": "turbo run dev --filter=web-audio-demos",
    "build": "turbo run build --filter=!drome-samples --filter=!repl-test",
    "build:core": "turbo run build --filter=drome-live",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "check-types:core": "turbo run check-types --filter=drome-live"
  },
  "keywords": [
    "drome",
    "dromelive",
    "tidalcycles",
    "pattern",
    "livecoding",
    "algorave"
  ],
  "packageManager": "pnpm@10.33.0",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.5.2",
    "prettier": "^3.8.1",
    "prettier-plugin-astro": "^0.14.1",
    "tsc-alias": "^1.8.16",
    "turbo": "^2.9.5",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
