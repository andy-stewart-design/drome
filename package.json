{
  "name": "drome",
  "private": false,
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --filter=!repl-test",
    "dev:repl": "turbo run dev --filter=repl-web",
    "dev:demos": "turbo run dev --filter=web-audio-demos",
    "build": "turbo run build --filter=!drome-samples --filter=!repl-test",
    "build:core": "turbo run build --filter=drome-live",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "keywords": [
    "drome",
    "dromelive",
    "tidalcycles",
    "pattern",
    "livecoding",
    "algorave"
  ],
  "packageManager": "pnpm@10.3.0",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prettier": "^3.6.2",
    "tsc-alias": "^1.8.16",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  }
}
